<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" >

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/rememberSwitchTitle"
        android:text="Remember What?"
        android:textAppearance="@android:style/TextAppearance.Large" />

    <Button
        android:background="@drawable/rectangle_white"
        android:layout_width="wrap_content"
        android:layout_height="350dp"
        android:id="@+id/rememberSwitchButton"
        android:minHeight="300dp"
        android:minWidth="300dp"
        android:layout_below="@+id/rememberSwitchTitle"
        android:layout_marginTop="20dp"

        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@android:style/TextAppearance.Large"
        android:text="Michi"
        android:id="@+id/rememberSwitchText"
        android:layout_below="@+id/rememberSwitchButton"
        android:layout_alignStart="@+id/rememberSwitchButton"
        android:layout_alignEnd="@+id/rememberSwitchButton"
        android:layout_marginTop="20dp"
        android:textAlignment="center" />


</RelativeLayout>
